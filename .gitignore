__pycache__/
*.py[cod]
.DS_Store

# Local virtualenv and runtime artifacts
spike-graphiti/.venv/
spike-graphiti/kuzu_db
.env
*.log

# Runtime data, imported memories, graph backups, snapshots, and watermarks.
# These can contain personal notes, crawl tokens, and large database files.
data/

# codegraph local index (do not commit)
.codegraph/

# 监控:真实 webhook 只留机器上,不入库
webhook.conf
**/webhook.conf
notify.json
dbpass.conf
**/dbpass.conf
